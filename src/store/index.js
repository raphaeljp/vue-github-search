import { createStore } from 'vuex'

const state = {}
const mutations = {}
const actions = {}
const getters = {}

const store = createStore({
    state,
    getters,
    actions,
    mutations
})

export default store